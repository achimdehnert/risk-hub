name: ⚙️ Maintenance / Task
description: Chores, technical debt, or infrastructure updates
title: "[TASK] "
labels: ["task", "triage"]
body:
  - type: textarea
    id: motivation
    attributes:
      label: 💡 Motivation
      description: Why are we doing this task?
    validations:
      required: true
  - type: textarea
    id: scope
    attributes:
      label: 🎯 Scope of Work
      description: What needs to be changed?
    validations:
      required: true
  - type: dropdown
    id: risk
    attributes:
      label: ⚠️ Risk Level
      options:
        - Low (No logic change)
        - Medium (Logic change, low impact)
        - High (Breaking change or data migration)
    validations:
      required: true
