# DEPRECATED: Replaced by ci.yml (ADR-090)
# This file is kept to avoid GitHub Actions errors on deletion.
# All CI/CD functionality is now in ci.yml.
name: "[DEPRECATED] Docker Build"
on:
  workflow_dispatch:
    inputs:
      note:
        description: "This workflow is deprecated. Use ci.yml instead."
        required: false
        type: string
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use ci.yml instead."
