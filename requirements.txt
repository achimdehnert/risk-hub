Django>=5.0,<6.0
dj-database-url>=2.2
psycopg[binary]>=3.2
python-decouple>=3.8
python-dotenv>=1.0
django-ninja>=1.1.0
djangorestframework>=3.15
django-filter>=24.0
pydantic>=2.7
boto3>=1.34
httpx>=0.27
structlog>=24.0
jinja2>=3.1
jsonschema>=4.21
tenacity>=8.2
weasyprint>=62.0
python-docx>=1.1
celery[redis]>=5.4
gunicorn>=22.0
whitenoise>=6.6
stripe>=7.0
# Platform shared packages — iil-platform umbrella (ADR-146 Phase 3)
iil-platform[shop]>=1.0.0
# ============================================================================
# iil-ecosystem packages (PRIMARY — do NOT use raw openai/anthropic directly)
# See: .windsurf/rules/iil-packages.md
# ============================================================================
iil-aifw>=0.5.0,<1
iil-fieldprefill>=0.2.0  # ADR-107 — installed from git in Dockerfile until PyPI publish is configured
iil-learnfw[api,tenancy]>=0.4.0
iil-promptfw>=0.7.0,<1
iil-authoringfw>=0.3.0,<1
# Concept Templates — ADR-147 (PDF extraction + LLM structure analysis)
iil-concept-templates>=0.4.0
# Document Templates — reusable Django template system
iil-doc-templates>=0.3.0
# REFLEX — UC quality checks + domain research (ADR-162)
iil-reflex[web]>=0.2.1
# Enrichment Agent Pattern — ADR-169 (GESTIS + PubChem providers)
# iil-enrichment[gestis,pubchem]>=0.1.0  # not yet on PyPI — optional, skipped at runtime
# Safety Framework — riskfw inlined in src/riskfw/ (ADR-146 Phase 1)
# CAD / BIM — nl2cad-core (ADR-146 Phase 2) — from git until PyPI publish
nl2cad-core @ git+https://github.com/achimdehnert/nl2cad.git#subdirectory=packages/nl2cad-core
mozilla-django-oidc>=4.0
openpyxl>=3.1
iil-ingest[pdf,ocr] @ git+https://github.com/achimdehnert/iil-ingest.git
requests>=2.31
